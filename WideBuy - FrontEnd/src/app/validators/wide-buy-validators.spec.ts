import { WideBuyValidators } from './wide-buy-validators';

describe('WideBuyValidators', () => {
  it('should create an instance', () => {
    expect(new WideBuyValidators()).toBeTruthy();
  });
});
